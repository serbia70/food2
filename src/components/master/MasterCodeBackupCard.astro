<section class="code-card code-card-backup">
  <div class="code-head">
    <div>
      <div class="code-title">代码版本备份</div>
      <div class="code-subtitle">创建、查看并删除代码备份版本，便于回溯重要发版节点。</div>
    </div>
  </div>

  <form class="code-create-row" onsubmit="return window.createMasterCodeBackup ? window.createMasterCodeBackup(this) : false;">
    <label>
      <span>备份名称</span>
      <input name="backupName" type="text" placeholder="例如：Master 备份与还原首版" />
    </label>
    <button type="submit" class="code-button">创建备份</button>
  </form>

  <div id="master-code-backup-feedback" class="code-feedback code-feedback-muted">
    页面加载后会自动读取代码备份列表。
  </div>

  <section class="code-list-panel">
    <div class="code-list-title">备份列表</div>
    <div id="master-code-backup-list" class="code-list">加载中...</div>
  </section>
</section>

<style>
  .code-card {
    background: #fff;
    border: 1px solid #d7dee7;
    border-radius: 18px;
    padding: 18px;
    box-shadow: 0 14px 36px rgba(15, 23, 42, 0.05);
  }

  .code-card-backup {
    border-left: 4px solid #7c3aed;
  }

  .code-head {
    margin-bottom: 14px;
  }

  .code-title {
    font-size: 20px;
    font-weight: 900;
    color: #0f172a;
  }

  .code-subtitle {
    margin-top: 4px;
    font-size: 13px;
    color: #64748b;
  }

  .code-create-row {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 14px;
    align-items: end;
  }

  .code-create-row label {
    display: grid;
    gap: 6px;
  }

  .code-create-row span {
    font-size: 12px;
    font-weight: 700;
    color: #475569;
  }

  .code-create-row input {
    width: 100%;
    border: 1px solid #cbd5e1;
    border-radius: 10px;
    padding: 10px 12px;
    font-size: 14px;
    background: #fff;
  }

  .code-button {
    border: none;
    border-radius: 10px;
    padding: 12px 16px;
    background: #7c3aed;
    color: #fff;
    font-weight: 800;
    cursor: pointer;
  }

  .code-feedback {
    margin-top: 12px;
    border-radius: 10px;
    padding: 10px 12px;
    font-size: 12px;
    line-height: 1.5;
  }

  .code-feedback-muted {
    background: #f8fafc;
    color: #64748b;
  }

  .code-list-panel {
    margin-top: 14px;
    display: grid;
    gap: 10px;
  }

  .code-list-title {
    font-size: 15px;
    font-weight: 800;
    color: #0f172a;
  }

  .code-list {
    min-height: 90px;
    border: 1px solid #e2e8f0;
    border-radius: 14px;
    background: #f8fafc;
    padding: 10px;
    color: #64748b;
  }

  @media (max-width: 860px) {
    .code-create-row {
      grid-template-columns: 1fr;
    }
  }
</style>
